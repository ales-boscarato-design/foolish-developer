# B2B Portal i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full UI translation (it, fr, en, es) to the B2B reseller portal using next-intl with cookie-based locale detection (no URL prefix changes).

**Architecture:** Cookie-based locale (`NEXT_LOCALE` cookie, set by next-intl middleware from `Accept-Language` header on first visit). No `[locale]` route prefix — the portal is private/noindex so SEO is irrelevant. A `LocaleSwitcher` component in the header lets users switch language. CMS API calls pass the detected locale to get localized product names/descriptions.

**Tech Stack:** next-intl ^4, Next.js 16.2.4, App Router, existing inline-style components (no Tailwind used for text).

---

## File Map

**Create:**
- `b2b/messages/it.json` — Italian strings (source of truth)
- `b2b/messages/fr.json` — French strings
- `b2b/messages/en.json` — English strings
- `b2b/messages/es.json` — Spanish strings
- `b2b/src/i18n/routing.ts` — locale list + localePrefix: 'never'
- `b2b/src/i18n/request.ts` — next-intl server config
- `b2b/src/middleware.ts` — next-intl middleware for cookie/Accept-Language detection
- `b2b/src/components/LocaleSwitcher.tsx` — header language switcher

**Modify:**
- `b2b/package.json` — add next-intl
- `b2b/next.config.ts` — wrap with createNextIntlPlugin
- `b2b/src/app/layout.tsx` — NextIntlClientProvider + dynamic lang + LocaleSwitcher
- `b2b/src/app/login/page.tsx` — useTranslations
- `b2b/src/app/carrello/page.tsx` — useTranslations
- `b2b/src/app/catalogo/page.tsx` — getTranslations + getLocale → CMS
- `b2b/src/app/catalogo/[slug]/page.tsx` — useTranslations + useLocale → ?locale= param
- `b2b/src/app/checkout/page.tsx` — useTranslations
- `b2b/src/app/checkout/conferma/page.tsx` — getTranslations
- `b2b/src/app/checkout/stripe-pay/page.tsx` — useTranslations + dynamic Stripe locale
- `b2b/src/app/account/page.tsx` — useTranslations
- `b2b/src/app/api/catalog/route.ts` — read locale from cookie, pass to CMS
- `b2b/src/lib/cms.ts` — add locale param to fetch functions
- `b2b/src/components/ProductCard.tsx` — useTranslations
- `b2b/src/components/KitCard.tsx` — useTranslations
- `b2b/src/components/PriceTierTable.tsx` — useTranslations

---

## Task 1: Install next-intl + scaffold i18n infrastructure

**Files:**
- Modify: `b2b/package.json`
- Create: `b2b/src/i18n/routing.ts`
- Create: `b2b/src/i18n/request.ts`
- Create: `b2b/src/middleware.ts`
- Modify: `b2b/next.config.ts`

- [ ] **Step 1: Install next-intl**

```bash
cd b2b && npm install next-intl
```

- [ ] **Step 2: Create routing config**

Create `b2b/src/i18n/routing.ts`:

```ts
import { defineRouting } from 'next-intl/routing'

export const routing = defineRouting({
  locales: ['it', 'fr', 'en', 'es'] as const,
  defaultLocale: 'it',
  localePrefix: 'never',
  localeDetection: true,
})

export type Locale = (typeof routing.locales)[number]
```

- [ ] **Step 3: Create request config**

Create `b2b/src/i18n/request.ts`:

```ts
import { getRequestConfig } from 'next-intl/server'
import { routing } from './routing'

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale
  if (!locale || !(routing.locales as readonly string[]).includes(locale)) {
    locale = routing.defaultLocale
  }
  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  }
})
```

- [ ] **Step 4: Create middleware**

Create `b2b/src/middleware.ts`:

```ts
import createMiddleware from 'next-intl/middleware'
import { routing } from './i18n/routing'

export default createMiddleware(routing)

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)'],
}
```

- [ ] **Step 5: Update next.config.ts**

Replace `b2b/next.config.ts` with:

```ts
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts')

const nextConfig: NextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'cms-production-1dda.up.railway.app' },
    ],
  },
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'X-Content-Type-Options', value: 'nosniff' },
        ],
      },
    ]
  },
}

export default withNextIntl(nextConfig)
```

- [ ] **Step 6: Typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors (messages files don't exist yet — that's fine, they're imported dynamically).

- [ ] **Step 7: Commit**

```bash
git add b2b/package.json b2b/package-lock.json b2b/src/i18n/routing.ts b2b/src/i18n/request.ts b2b/src/middleware.ts b2b/next.config.ts
git commit -m "feat(b2b/i18n): install next-intl, scaffold routing/middleware"
```

---

## Task 2: Create translation files

**Files:**
- Create: `b2b/messages/it.json`
- Create: `b2b/messages/fr.json`
- Create: `b2b/messages/en.json`
- Create: `b2b/messages/es.json`

- [ ] **Step 1: Create Italian (source of truth)**

Create `b2b/messages/it.json`:

```json
{
  "Layout": {
    "areaRivenditori": "Area Rivenditori",
    "catalogo": "Catalogo",
    "carrello": "Carrello",
    "account": "Account",
    "esci": "Esci"
  },
  "Login": {
    "portaleRivenditori": "Portale Rivenditori",
    "errors": {
      "invalid": "Link non valido.",
      "expired": "Link scaduto. Richiedi un nuovo accesso.",
      "unauthorized": "Account non autorizzato o sospeso.",
      "unknown": "Errore sconosciuto."
    },
    "emailInviata": "Email inviata",
    "emailInviataDesc": "Se l'email è registrata, riceverai il link di accesso entro pochi secondi.",
    "accedi": "Accedi",
    "desc": "Inserisci la tua email. Ti invieremo un link di accesso sicuro.",
    "placeholder": "email@azienda.it",
    "invioInCorso": "Invio in corso…",
    "riceviLink": "Ricevi link di accesso",
    "accessoRiservato": "Accesso riservato ai rivenditori autorizzati"
  },
  "Catalogo": {
    "portaleRivenditori": "Portale Rivenditori",
    "heroTitle": "Ogni foglio di pelle, un'opera irripetibile.",
    "heroBody": "The Foolish Butcher produce pelli sintetiche per tatuaggio artigianalmente, in piccoli lotti curati uno ad uno. I tempi di consegna sono leggermente superiori alla produzione industriale — non per inefficienza, ma per scelta. Ogni lotto cambia per consistenza e tonalità: questo è il segreto che trasforma i tuoi clienti in",
    "habitueSuffix": "habitué",
    "pillars": {
      "p1icon": "✦",
      "p1title": "Produzione artigianale",
      "p1body": "Ogni foglio è lavorato a mano nel nostro laboratorio in piccoli lotti. Non produciamo in serie — e si vede.",
      "p2icon": "◈",
      "p2title": "Ogni lotto, unico",
      "p2body": "Consistenza e colorazione variano naturalmente tra un lotto e l'altro. I tuoi clienti tatueranno sempre su qualcosa di leggermente diverso.",
      "p3icon": "⟳",
      "p3title": "Clienti fedeli garantiti",
      "p3body": "Un prodotto che cambia è un prodotto che non annoia. Chi tatua su Foolish torna per tatuare ancora su Foolish."
    },
    "nessuniProdotti": "Nessun prodotto disponibile al momento.",
    "tattooDesc": "Pelli sintetiche per pratica e formazione nel tatuaggio",
    "pmuDesc": "Supporti per Permanent Make-up — sopracciglia, labbra, eyeliner",
    "prodotto": "prodotto",
    "prodotti": "prodotti",
    "kitTitle": "Kit per iniziare",
    "kitBadge": "Solo rivenditori",
    "kitDesc": "Prova i nostri prodotti prima di ordinare in quantità — scontati e riservati ai partner.",
    "infoTitle": "Informazioni per i rivenditori",
    "info1Title": "Come lavoriamo",
    "info1Body1": "Non lavoriamo a magazzino. Ogni ordine viene preparato nel momento in cui arriva: la pelle viene realizzata giornalmente, e giorno per giorno calibriamo consistenza e colore. Così ricevi sempre un prodotto fresco, mai fermo a scaffale per mesi.",
    "info1Body2": "Proprio perché ogni pelle nasce su ordinazione e viene controllata una a una, la spedizione richiede in media circa",
    "info1Days": "10 giorni lavorativi",
    "info1Body3": ": è il tempo che ci serve per realizzarla a regola d'arte.",
    "info1Body4": "Ogni pelle rispetta i nostri standard di produzione più alti e viene controllata per essere esente da difetti. Lavorando a mano, però, può capitare che qualcosa ci sfugga: in quel caso la sostituzione è immediata e garantita, senza discussioni.",
    "info2Title": "Cosa possiamo fare per il rivenditore di fiducia",
    "info2Body": "Scegliamo i nostri rivenditori con cura, in base al territorio e alla continuità dei riordini. Producendo in modo artigianale, non puntiamo a coprire ogni angolo del mercato: preferiamo pochi partner motivati, che condividano la nostra idea di qualità e capiscano cosa una buona pelle sintetica deve davvero offrire a chi pratica.",
    "info3Title": "Il packaging: a marchio Foolish o su misura",
    "info3Body1": "I nostri prodotti arrivano con il packaging Foolish. Per molti rivenditori è un valore aggiunto: un marchio riconoscibile aiuta a vendere e dà credibilità al prodotto sul banco.",
    "info3Body2": "Se invece preferisci proteggere il rapporto diretto con i tuoi clienti finali, possiamo lavorare in white label o personalizzare il packaging sulle tue esigenze. Decidi tu come presentarti — ne parliamo insieme e troviamo la formula giusta."
  },
  "ProductPage": {
    "tornaCatalogo": "← Catalogo",
    "prodottoNonTrovato": "Prodotto non trovato.",
    "variante": "Variante",
    "quantita": "Quantità",
    "aggiunto": "✓ Aggiunto!",
    "aggiungiCarrello": "Aggiungi al carrello",
    "caricamento": "Caricamento..."
  },
  "ProductCard": {
    "nessunaImmagine": "Nessuna immagine",
    "da": "Da",
    "alPezzo": "/pz",
    "finoA": "Fino a -{n}%"
  },
  "KitCard": {
    "kitRivenditori": "Kit rivenditori",
    "piuRichiesto": "Più richiesto",
    "aggiunto": "✓ Aggiunto al carrello",
    "aggiungiCarrello": "Aggiungi al carrello"
  },
  "PriceTierTable": {
    "prezziVolume": "Prezzi a volume",
    "quantita": "Quantità",
    "sconto": "Sconto",
    "prezzoPz": "Prezzo/pz",
    "rangePz": "{min}–{max} pz",
    "minPlusPz": "{min}+ pz"
  },
  "Carrello": {
    "vuoto": "Il carrello è vuoto.",
    "tornaCatalogo": "Torna al catalogo",
    "titolo": "Carrello",
    "totaleIva": "Totale (IVA inclusa)",
    "spedizione": "+ spedizione calcolata al checkout",
    "checkout": "Checkout →"
  },
  "Checkout": {
    "titolo": "Checkout",
    "fatturazione": "Dati fatturazione",
    "ragioneSociale": "Ragione sociale *",
    "piva": "P.IVA *",
    "sdi": "Codice SDI / PEC *",
    "sdiPlaceholder": "0000000 o pec@email.it",
    "indirizzoBilling": "Indirizzo di fatturazione *",
    "citta": "Città *",
    "cap": "CAP *",
    "spedizione": "Indirizzo di spedizione",
    "nomeDestinatario": "Nome / Ragione sociale destinatario *",
    "indirizzoSpedizione": "Indirizzo *",
    "pagamento": "Metodo di pagamento",
    "bonifico": "Bonifico bancario",
    "bonificoDesc": "Riceverai le coordinate al momento della conferma",
    "carta": "Carta di credito (Stripe)",
    "cartaDesc": "Supplemento del 4% per pagamento con carta. Totale con carta: {price}",
    "note": "Note (opzionale)",
    "notePlaceholder": "Note per l'ordine, istruzioni di consegna, ecc.",
    "subtotale": "Subtotale prodotti",
    "supplementoCarta": "Supplemento carta (4%)",
    "costoSpedizione": "Spedizione",
    "daDefinire": "Da definire",
    "totale": "Totale",
    "elaborazione": "Elaborazione...",
    "confermaBonifico": "Conferma ordine (bonifico bancario)",
    "pagaCarta": "Paga con carta →",
    "errorOrdine": "Errore durante la creazione dell'ordine. Riprova."
  },
  "Conferma": {
    "ordineConfermato": "Ordine confermato",
    "numeroOrdine": "Numero ordine:",
    "coordinateBonifico": "Coordinate per il bonifico:",
    "intestatario": "Intestatario:",
    "iban": "IBAN:",
    "causale": "Causale: {ordine} — inserisci il numero d'ordine come causale.",
    "pagamentoRicevuto": "Il pagamento è stato ricevuto.",
    "emailConferma": "Hai ricevuto la conferma d'ordine via email.",
    "continuaAcquistare": "Continua ad acquistare"
  },
  "StripePay": {
    "caricamento": "Caricamento...",
    "titolo": "Pagamento con carta",
    "elaborazione": "Elaborazione...",
    "pagaOra": "Paga ora",
    "errore": "Errore pagamento"
  },
  "Account": {
    "caricamento": "Caricamento...",
    "titolo": "Il mio account",
    "profiloAziendale": "Profilo aziendale",
    "ragioneSociale": "Ragione sociale",
    "referente": "Referente",
    "email": "Email",
    "piva": "P.IVA",
    "storicoOrdini": "Storico ordini",
    "nessunoOrdine": "Nessun ordine ancora.",
    "states": {
      "received": "Ricevuto",
      "in_production": "In produzione",
      "shipped": "Spedito",
      "delivered": "Consegnato"
    },
    "tracking": "Tracking:"
  }
}
```

- [ ] **Step 2: Create French**

Create `b2b/messages/fr.json`:

```json
{
  "Layout": {
    "areaRivenditori": "Espace Revendeurs",
    "catalogo": "Catalogue",
    "carrello": "Panier",
    "account": "Compte",
    "esci": "Déconnexion"
  },
  "Login": {
    "portaleRivenditori": "Portail Revendeurs",
    "errors": {
      "invalid": "Lien invalide.",
      "expired": "Lien expiré. Demandez un nouvel accès.",
      "unauthorized": "Compte non autorisé ou suspendu.",
      "unknown": "Erreur inconnue."
    },
    "emailInviata": "E-mail envoyé",
    "emailInviataDesc": "Si l'adresse e-mail est enregistrée, vous recevrez le lien d'accès dans quelques secondes.",
    "accedi": "Connexion",
    "desc": "Saisissez votre adresse e-mail. Nous vous enverrons un lien d'accès sécurisé.",
    "placeholder": "email@entreprise.fr",
    "invioInCorso": "Envoi en cours…",
    "riceviLink": "Recevoir le lien d'accès",
    "accessoRiservato": "Accès réservé aux revendeurs autorisés"
  },
  "Catalogo": {
    "portaleRivenditori": "Portail Revendeurs",
    "heroTitle": "Chaque feuille de peau, une œuvre unique.",
    "heroBody": "The Foolish Butcher produit des peaux synthétiques pour le tatouage de manière artisanale, en petits lots soignés un à un. Les délais de livraison sont légèrement supérieurs à la production industrielle — non par inefficacité, mais par choix. Chaque lot varie en consistance et en teinte : c'est le secret qui transforme vos clients en",
    "habitueSuffix": "habitués",
    "pillars": {
      "p1icon": "✦",
      "p1title": "Production artisanale",
      "p1body": "Chaque feuille est travaillée à la main dans notre atelier en petits lots. Nous ne produisons pas en série — et ça se voit.",
      "p2icon": "◈",
      "p2title": "Chaque lot, unique",
      "p2body": "La consistance et la coloration varient naturellement d'un lot à l'autre. Vos clients tatouent toujours sur quelque chose de légèrement différent.",
      "p3icon": "⟳",
      "p3title": "Clients fidèles garantis",
      "p3body": "Un produit qui change est un produit qui ne lasse pas. Ceux qui tatouent sur Foolish reviennent pour tatouer encore sur Foolish."
    },
    "nessuniProdotti": "Aucun produit disponible pour le moment.",
    "tattooDesc": "Peaux synthétiques pour la pratique et la formation en tatouage",
    "pmuDesc": "Supports pour le maquillage permanent — sourcils, lèvres, eye-liner",
    "prodotto": "produit",
    "prodotti": "produits",
    "kitTitle": "Kits pour commencer",
    "kitBadge": "Revendeurs uniquement",
    "kitDesc": "Testez nos produits avant de commander en grande quantité — remisés et réservés aux partenaires.",
    "infoTitle": "Informations pour les revendeurs",
    "info1Title": "Notre méthode de travail",
    "info1Body1": "Nous ne travaillons pas sur stock. Chaque commande est préparée au moment où elle arrive : la peau est fabriquée quotidiennement, et chaque jour nous calibrons la consistance et la couleur. Vous recevez ainsi toujours un produit frais, jamais immobilisé en rayon pendant des mois.",
    "info1Body2": "Parce que chaque peau est fabriquée sur commande et contrôlée une à une, la livraison prend en moyenne environ",
    "info1Days": "10 jours ouvrables",
    "info1Body3": ": c'est le temps qu'il nous faut pour la réaliser dans les règles de l'art.",
    "info1Body4": "Chaque peau respecte nos standards de production les plus élevés et est contrôlée pour être exempte de défauts. En travaillant à la main, il peut arriver que quelque chose nous échappe : dans ce cas, le remplacement est immédiat et garanti, sans discussion.",
    "info2Title": "Ce que nous pouvons faire pour notre revendeur de confiance",
    "info2Body": "Nous choisissons nos revendeurs avec soin, en fonction du territoire et de la régularité des réassorts. En produisant de manière artisanale, nous ne cherchons pas à couvrir chaque recoin du marché : nous préférons peu de partenaires motivés, partageant notre conception de la qualité.",
    "info3Title": "Le packaging : marque Foolish ou sur mesure",
    "info3Body1": "Nos produits arrivent avec le packaging Foolish. Pour de nombreux revendeurs, c'est une valeur ajoutée : une marque reconnaissable aide à vendre et donne de la crédibilité au produit.",
    "info3Body2": "Si vous préférez protéger la relation directe avec vos clients finaux, nous pouvons travailler en marque blanche ou personnaliser le packaging selon vos besoins. C'est vous qui décidez — nous en parlons ensemble et trouvons la bonne formule."
  },
  "ProductPage": {
    "tornaCatalogo": "← Catalogue",
    "prodottoNonTrovato": "Produit introuvable.",
    "variante": "Variante",
    "quantita": "Quantité",
    "aggiunto": "✓ Ajouté !",
    "aggiungiCarrello": "Ajouter au panier",
    "caricamento": "Chargement..."
  },
  "ProductCard": {
    "nessunaImmagine": "Aucune image",
    "da": "À partir de",
    "alPezzo": "/pcs",
    "finoA": "Jusqu'à -{n}%"
  },
  "KitCard": {
    "kitRivenditori": "Kit revendeurs",
    "piuRichiesto": "Le plus demandé",
    "aggiunto": "✓ Ajouté au panier",
    "aggiungiCarrello": "Ajouter au panier"
  },
  "PriceTierTable": {
    "prezziVolume": "Tarifs dégressifs",
    "quantita": "Quantité",
    "sconto": "Remise",
    "prezzoPz": "Prix/pcs",
    "rangePz": "{min}–{max} pcs",
    "minPlusPz": "{min}+ pcs"
  },
  "Carrello": {
    "vuoto": "Votre panier est vide.",
    "tornaCatalogo": "Retour au catalogue",
    "titolo": "Panier",
    "totaleIva": "Total (TVA incluse)",
    "spedizione": "+ frais de port calculés au paiement",
    "checkout": "Commander →"
  },
  "Checkout": {
    "titolo": "Commande",
    "fatturazione": "Données de facturation",
    "ragioneSociale": "Raison sociale *",
    "piva": "N° TVA intracommunautaire *",
    "sdi": "Référence facturation électronique *",
    "sdiPlaceholder": "Réf. EDI ou email comptabilité",
    "indirizzoBilling": "Adresse de facturation *",
    "citta": "Ville *",
    "cap": "Code postal *",
    "spedizione": "Adresse de livraison",
    "nomeDestinatario": "Nom / Raison sociale destinataire *",
    "indirizzoSpedizione": "Adresse *",
    "pagamento": "Mode de paiement",
    "bonifico": "Virement bancaire",
    "bonificoDesc": "Vous recevrez les coordonnées bancaires à la confirmation",
    "carta": "Carte de crédit (Stripe)",
    "cartaDesc": "Supplément de 4 % pour paiement par carte. Total avec carte : {price}",
    "note": "Notes (optionnel)",
    "notePlaceholder": "Notes pour la commande, instructions de livraison, etc.",
    "subtotale": "Sous-total produits",
    "supplementoCarta": "Supplément carte (4 %)",
    "costoSpedizione": "Livraison",
    "daDefinire": "À définir",
    "totale": "Total",
    "elaborazione": "Traitement en cours...",
    "confermaBonifico": "Confirmer la commande (virement bancaire)",
    "pagaCarta": "Payer par carte →",
    "errorOrdine": "Erreur lors de la création de la commande. Veuillez réessayer."
  },
  "Conferma": {
    "ordineConfermato": "Commande confirmée",
    "numeroOrdine": "Numéro de commande :",
    "coordinateBonifico": "Coordonnées pour le virement :",
    "intestatario": "Bénéficiaire :",
    "iban": "IBAN :",
    "causale": "Référence : {ordine} — indiquez le numéro de commande comme référence.",
    "pagamentoRicevuto": "Le paiement a été reçu.",
    "emailConferma": "Vous avez reçu la confirmation de commande par e-mail.",
    "continuaAcquistare": "Continuer mes achats"
  },
  "StripePay": {
    "caricamento": "Chargement...",
    "titolo": "Paiement par carte",
    "elaborazione": "Traitement en cours...",
    "pagaOra": "Payer maintenant",
    "errore": "Erreur de paiement"
  },
  "Account": {
    "caricamento": "Chargement...",
    "titolo": "Mon compte",
    "profiloAziendale": "Profil entreprise",
    "ragioneSociale": "Raison sociale",
    "referente": "Référent",
    "email": "E-mail",
    "piva": "N° TVA",
    "storicoOrdini": "Historique des commandes",
    "nessunoOrdine": "Aucune commande pour le moment.",
    "states": {
      "received": "Reçue",
      "in_production": "En production",
      "shipped": "Expédiée",
      "delivered": "Livrée"
    },
    "tracking": "Suivi :"
  }
}
```

- [ ] **Step 3: Create English**

Create `b2b/messages/en.json`:

```json
{
  "Layout": {
    "areaRivenditori": "Reseller Area",
    "catalogo": "Catalogue",
    "carrello": "Cart",
    "account": "Account",
    "esci": "Logout"
  },
  "Login": {
    "portaleRivenditori": "Reseller Portal",
    "errors": {
      "invalid": "Invalid link.",
      "expired": "Link expired. Request a new access link.",
      "unauthorized": "Account not authorised or suspended.",
      "unknown": "Unknown error."
    },
    "emailInviata": "Email sent",
    "emailInviataDesc": "If the email address is registered, you will receive the access link within a few seconds.",
    "accedi": "Sign in",
    "desc": "Enter your email address. We will send you a secure access link.",
    "placeholder": "email@company.com",
    "invioInCorso": "Sending…",
    "riceviLink": "Get access link",
    "accessoRiservato": "Access reserved for authorised resellers"
  },
  "Catalogo": {
    "portaleRivenditori": "Reseller Portal",
    "heroTitle": "Every sheet of skin, an unrepeatable work.",
    "heroBody": "The Foolish Butcher crafts synthetic tattoo skins by hand, in small batches cared for one by one. Delivery times are slightly longer than industrial production — not out of inefficiency, but by choice. Every batch varies in consistency and tone: this is the secret that turns your clients into",
    "habitueSuffix": "regulars",
    "pillars": {
      "p1icon": "✦",
      "p1title": "Artisan production",
      "p1body": "Every sheet is hand-crafted in our workshop in small batches. We don't mass produce — and it shows.",
      "p2icon": "◈",
      "p2title": "Every batch, unique",
      "p2body": "Consistency and colour vary naturally from batch to batch. Your clients always tattoo on something slightly different.",
      "p3icon": "⟳",
      "p3title": "Loyal customers guaranteed",
      "p3body": "A product that changes is a product that doesn't bore. Those who tattoo on Foolish come back to tattoo on Foolish again."
    },
    "nessuniProdotti": "No products available at the moment.",
    "tattooDesc": "Synthetic skins for tattoo practice and training",
    "pmuDesc": "Supports for Permanent Make-up — eyebrows, lips, eyeliner",
    "prodotto": "product",
    "prodotti": "products",
    "kitTitle": "Starter kits",
    "kitBadge": "Resellers only",
    "kitDesc": "Try our products before ordering in bulk — discounted and reserved for partners.",
    "infoTitle": "Information for resellers",
    "info1Title": "How we work",
    "info1Body1": "We don't work from stock. Every order is prepared as it arrives: skin is made daily, and each day we calibrate consistency and colour. You always receive a fresh product, never sitting on a shelf for months.",
    "info1Body2": "Because every skin is made to order and checked one by one, shipping takes on average around",
    "info1Days": "10 working days",
    "info1Body3": ": that is the time we need to craft it properly.",
    "info1Body4": "Every skin meets our highest production standards and is checked to be free of defects. Working by hand, something can occasionally slip through: in that case, replacement is immediate and guaranteed, no questions asked.",
    "info2Title": "What we can do for our trusted reseller",
    "info2Body": "We choose our resellers carefully, based on territory and regularity of reorders. By producing artisanally, we don't aim to cover every corner of the market: we prefer a few motivated partners who share our idea of quality.",
    "info3Title": "Packaging: Foolish brand or custom",
    "info3Body1": "Our products arrive with Foolish packaging. For many resellers this is added value: a recognisable brand helps sell and gives credibility to the product.",
    "info3Body2": "If you prefer to protect your direct relationship with end customers, we can work white label or customise the packaging to your needs. You decide how to present yourself — we'll discuss it and find the right formula."
  },
  "ProductPage": {
    "tornaCatalogo": "← Catalogue",
    "prodottoNonTrovato": "Product not found.",
    "variante": "Variant",
    "quantita": "Quantity",
    "aggiunto": "✓ Added!",
    "aggiungiCarrello": "Add to cart",
    "caricamento": "Loading..."
  },
  "ProductCard": {
    "nessunaImmagine": "No image",
    "da": "From",
    "alPezzo": "/pc",
    "finoA": "Up to -{n}%"
  },
  "KitCard": {
    "kitRivenditori": "Reseller kit",
    "piuRichiesto": "Most popular",
    "aggiunto": "✓ Added to cart",
    "aggiungiCarrello": "Add to cart"
  },
  "PriceTierTable": {
    "prezziVolume": "Volume pricing",
    "quantita": "Quantity",
    "sconto": "Discount",
    "prezzoPz": "Price/pc",
    "rangePz": "{min}–{max} pcs",
    "minPlusPz": "{min}+ pcs"
  },
  "Carrello": {
    "vuoto": "Your cart is empty.",
    "tornaCatalogo": "Back to catalogue",
    "titolo": "Cart",
    "totaleIva": "Total (VAT included)",
    "spedizione": "+ shipping calculated at checkout",
    "checkout": "Checkout →"
  },
  "Checkout": {
    "titolo": "Checkout",
    "fatturazione": "Billing details",
    "ragioneSociale": "Company name *",
    "piva": "VAT number *",
    "sdi": "Electronic invoice reference *",
    "sdiPlaceholder": "EDI code or accounting email",
    "indirizzoBilling": "Billing address *",
    "citta": "City *",
    "cap": "Postcode *",
    "spedizione": "Shipping address",
    "nomeDestinatario": "Name / Company name (recipient) *",
    "indirizzoSpedizione": "Address *",
    "pagamento": "Payment method",
    "bonifico": "Bank transfer",
    "bonificoDesc": "You will receive bank details upon confirmation",
    "carta": "Credit card (Stripe)",
    "cartaDesc": "4% surcharge for card payment. Total with card: {price}",
    "note": "Notes (optional)",
    "notePlaceholder": "Order notes, delivery instructions, etc.",
    "subtotale": "Product subtotal",
    "supplementoCarta": "Card surcharge (4%)",
    "costoSpedizione": "Shipping",
    "daDefinire": "To be confirmed",
    "totale": "Total",
    "elaborazione": "Processing...",
    "confermaBonifico": "Confirm order (bank transfer)",
    "pagaCarta": "Pay by card →",
    "errorOrdine": "Error creating the order. Please try again."
  },
  "Conferma": {
    "ordineConfermato": "Order confirmed",
    "numeroOrdine": "Order number:",
    "coordinateBonifico": "Bank transfer details:",
    "intestatario": "Beneficiary:",
    "iban": "IBAN:",
    "causale": "Reference: {ordine} — please use the order number as payment reference.",
    "pagamentoRicevuto": "Payment received.",
    "emailConferma": "You have received the order confirmation by email.",
    "continuaAcquistare": "Continue shopping"
  },
  "StripePay": {
    "caricamento": "Loading...",
    "titolo": "Card payment",
    "elaborazione": "Processing...",
    "pagaOra": "Pay now",
    "errore": "Payment error"
  },
  "Account": {
    "caricamento": "Loading...",
    "titolo": "My account",
    "profiloAziendale": "Company profile",
    "ragioneSociale": "Company name",
    "referente": "Contact",
    "email": "Email",
    "piva": "VAT number",
    "storicoOrdini": "Order history",
    "nessunoOrdine": "No orders yet.",
    "states": {
      "received": "Received",
      "in_production": "In production",
      "shipped": "Shipped",
      "delivered": "Delivered"
    },
    "tracking": "Tracking:"
  }
}
```

- [ ] **Step 4: Create Spanish**

Create `b2b/messages/es.json`:

```json
{
  "Layout": {
    "areaRivenditori": "Área Revendedores",
    "catalogo": "Catálogo",
    "carrello": "Carrito",
    "account": "Cuenta",
    "esci": "Cerrar sesión"
  },
  "Login": {
    "portaleRivenditori": "Portal Revendedores",
    "errors": {
      "invalid": "Enlace no válido.",
      "expired": "Enlace caducado. Solicite un nuevo acceso.",
      "unauthorized": "Cuenta no autorizada o suspendida.",
      "unknown": "Error desconocido."
    },
    "emailInviata": "Email enviado",
    "emailInviataDesc": "Si el email está registrado, recibirá el enlace de acceso en unos segundos.",
    "accedi": "Acceder",
    "desc": "Introduzca su email. Le enviaremos un enlace de acceso seguro.",
    "placeholder": "email@empresa.es",
    "invioInCorso": "Enviando…",
    "riceviLink": "Recibir enlace de acceso",
    "accessoRiservato": "Acceso reservado a revendedores autorizados"
  },
  "Catalogo": {
    "portaleRivenditori": "Portal Revendedores",
    "heroTitle": "Cada hoja de piel, una obra irrepetible.",
    "heroBody": "The Foolish Butcher produce pieles sintéticas para tatuaje de forma artesanal, en pequeños lotes cuidados uno a uno. Los plazos de entrega son ligeramente superiores a la producción industrial — no por ineficiencia, sino por elección. Cada lote varía en consistencia y tonalidad: este es el secreto que convierte a sus clientes en",
    "habitueSuffix": "habituales",
    "pillars": {
      "p1icon": "✦",
      "p1title": "Producción artesanal",
      "p1body": "Cada hoja se trabaja a mano en nuestro taller en pequeños lotes. No producimos en serie — y se nota.",
      "p2icon": "◈",
      "p2title": "Cada lote, único",
      "p2body": "La consistencia y la coloración varían de forma natural entre un lote y otro. Sus clientes siempre tatuarán sobre algo ligeramente diferente.",
      "p3icon": "⟳",
      "p3title": "Clientes fieles garantizados",
      "p3body": "Un producto que cambia es un producto que no aburre. Quien tatúa en Foolish vuelve para tatuar de nuevo en Foolish."
    },
    "nessuniProdotti": "No hay productos disponibles en este momento.",
    "tattooDesc": "Pieles sintéticas para práctica y formación en tatuaje",
    "pmuDesc": "Soportes para maquillaje permanente — cejas, labios, eye-liner",
    "prodotto": "producto",
    "prodotti": "productos",
    "kitTitle": "Kits para empezar",
    "kitBadge": "Solo revendedores",
    "kitDesc": "Pruebe nuestros productos antes de pedir en cantidad — con descuento y reservados a los socios.",
    "infoTitle": "Información para revendedores",
    "info1Title": "Cómo trabajamos",
    "info1Body1": "No trabajamos con stock. Cada pedido se prepara en el momento en que llega: la piel se fabrica a diario, y cada día calibramos consistencia y color. Así recibe siempre un producto fresco, nunca parado en un estante durante meses.",
    "info1Body2": "Precisamente porque cada piel se fabrica bajo pedido y se controla una a una, el envío requiere en promedio unos",
    "info1Days": "10 días laborables",
    "info1Body3": ": es el tiempo que necesitamos para realizarla a la perfección.",
    "info1Body4": "Cada piel respeta nuestros más altos estándares de producción y se controla para estar libre de defectos. Trabajando a mano, puede ocurrir que algo se nos escape: en ese caso, la sustitución es inmediata y garantizada, sin discusión.",
    "info2Title": "Qué podemos hacer por el revendedor de confianza",
    "info2Body": "Elegimos a nuestros revendedores con cuidado, en función del territorio y la continuidad de los pedidos. Produciendo de forma artesanal, no pretendemos cubrir cada rincón del mercado: preferimos pocos socios motivados que compartan nuestra idea de calidad.",
    "info3Title": "El packaging: marca Foolish o a medida",
    "info3Body1": "Nuestros productos llegan con el packaging Foolish. Para muchos revendedores es un valor añadido: una marca reconocible ayuda a vender y da credibilidad al producto.",
    "info3Body2": "Si prefiere proteger la relación directa con sus clientes finales, podemos trabajar en marca blanca o personalizar el packaging según sus necesidades. Usted decide cómo presentarse — lo hablamos juntos y encontramos la fórmula adecuada."
  },
  "ProductPage": {
    "tornaCatalogo": "← Catálogo",
    "prodottoNonTrovato": "Producto no encontrado.",
    "variante": "Variante",
    "quantita": "Cantidad",
    "aggiunto": "✓ ¡Añadido!",
    "aggiungiCarrello": "Añadir al carrito",
    "caricamento": "Cargando..."
  },
  "ProductCard": {
    "nessunaImmagine": "Sin imagen",
    "da": "Desde",
    "alPezzo": "/ud.",
    "finoA": "Hasta -{n}%"
  },
  "KitCard": {
    "kitRivenditori": "Kit revendedores",
    "piuRichiesto": "Más solicitado",
    "aggiunto": "✓ Añadido al carrito",
    "aggiungiCarrello": "Añadir al carrito"
  },
  "PriceTierTable": {
    "prezziVolume": "Precios por volumen",
    "quantita": "Cantidad",
    "sconto": "Descuento",
    "prezzoPz": "Precio/ud.",
    "rangePz": "{min}–{max} ud.",
    "minPlusPz": "{min}+ ud."
  },
  "Carrello": {
    "vuoto": "El carrito está vacío.",
    "tornaCatalogo": "Volver al catálogo",
    "titolo": "Carrito",
    "totaleIva": "Total (IVA incluido)",
    "spedizione": "+ gastos de envío calculados al pagar",
    "checkout": "Pagar →"
  },
  "Checkout": {
    "titolo": "Pago",
    "fatturazione": "Datos de facturación",
    "ragioneSociale": "Razón social *",
    "piva": "N.º IVA *",
    "sdi": "Referencia factura electrónica *",
    "sdiPlaceholder": "Código EDI o email contabilidad",
    "indirizzoBilling": "Dirección de facturación *",
    "citta": "Ciudad *",
    "cap": "Código postal *",
    "spedizione": "Dirección de envío",
    "nomeDestinatario": "Nombre / Razón social destinatario *",
    "indirizzoSpedizione": "Dirección *",
    "pagamento": "Método de pago",
    "bonifico": "Transferencia bancaria",
    "bonificoDesc": "Recibirá los datos bancarios en la confirmación",
    "carta": "Tarjeta de crédito (Stripe)",
    "cartaDesc": "Suplemento del 4 % por pago con tarjeta. Total con tarjeta: {price}",
    "note": "Notas (opcional)",
    "notePlaceholder": "Notas del pedido, instrucciones de entrega, etc.",
    "subtotale": "Subtotal productos",
    "supplementoCarta": "Suplemento tarjeta (4 %)",
    "costoSpedizione": "Envío",
    "daDefinire": "Por determinar",
    "totale": "Total",
    "elaborazione": "Procesando...",
    "confermaBonifico": "Confirmar pedido (transferencia bancaria)",
    "pagaCarta": "Pagar con tarjeta →",
    "errorOrdine": "Error al crear el pedido. Por favor, inténtelo de nuevo."
  },
  "Conferma": {
    "ordineConfermato": "Pedido confirmado",
    "numeroOrdine": "Número de pedido:",
    "coordinateBonifico": "Datos para la transferencia:",
    "intestatario": "Beneficiario:",
    "iban": "IBAN:",
    "causale": "Concepto: {ordine} — indique el número de pedido como concepto.",
    "pagamentoRicevuto": "Pago recibido.",
    "emailConferma": "Ha recibido la confirmación del pedido por email.",
    "continuaAcquistare": "Seguir comprando"
  },
  "StripePay": {
    "caricamento": "Cargando...",
    "titolo": "Pago con tarjeta",
    "elaborazione": "Procesando...",
    "pagaOra": "Pagar ahora",
    "errore": "Error de pago"
  },
  "Account": {
    "caricamento": "Cargando...",
    "titolo": "Mi cuenta",
    "profiloAziendale": "Perfil empresarial",
    "ragioneSociale": "Razón social",
    "referente": "Contacto",
    "email": "Email",
    "piva": "N.º IVA",
    "storicoOrdini": "Historial de pedidos",
    "nessunoOrdine": "Aún no hay pedidos.",
    "states": {
      "received": "Recibido",
      "in_production": "En producción",
      "shipped": "Enviado",
      "delivered": "Entregado"
    },
    "tracking": "Seguimiento:"
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add b2b/messages/
git commit -m "feat(b2b/i18n): translation files — it, fr, en, es"
```

---

## Task 3: LocaleSwitcher + updated layout

**Files:**
- Create: `b2b/src/components/LocaleSwitcher.tsx`
- Modify: `b2b/src/app/layout.tsx`

- [ ] **Step 1: Create LocaleSwitcher**

Create `b2b/src/components/LocaleSwitcher.tsx`:

```tsx
'use client'
import { useLocale } from 'next-intl'

const LOCALES = [
  { code: 'it', label: 'IT' },
  { code: 'fr', label: 'FR' },
  { code: 'en', label: 'EN' },
  { code: 'es', label: 'ES' },
]

export function LocaleSwitcher() {
  const locale = useLocale()

  function switchLocale(code: string) {
    document.cookie = `NEXT_LOCALE=${code}; path=/; max-age=31536000; SameSite=Lax`
    window.location.reload()
  }

  return (
    <div style={{ display: 'flex', gap: '0.25rem', alignItems: 'center', marginLeft: '0.5rem', borderLeft: '1px solid var(--border)', paddingLeft: '0.75rem' }}>
      {LOCALES.map(({ code, label }) => (
        <button
          key={code}
          onClick={() => switchLocale(code)}
          style={{
            background: 'transparent',
            border: 'none',
            padding: '0.25rem 0.375rem',
            fontSize: '0.7rem',
            fontWeight: code === locale ? 600 : 400,
            color: code === locale ? 'var(--accent)' : 'var(--muted-fg)',
            cursor: code === locale ? 'default' : 'pointer',
            letterSpacing: '0.08em',
            borderRadius: '0.25rem',
            opacity: code === locale ? 1 : 0.6,
            transition: 'color var(--dur-fast), opacity var(--dur-fast)',
          }}
          disabled={code === locale}
          aria-label={`Switch to ${label}`}
          aria-current={code === locale ? 'true' : undefined}
        >
          {label}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Update layout**

Replace `b2b/src/app/layout.tsx` with:

```tsx
import type { Metadata } from 'next'
import { Outfit, Cormorant_Garamond } from 'next/font/google'
import { NextIntlClientProvider } from 'next-intl'
import { getLocale, getMessages, getTranslations } from 'next-intl/server'
import { LocaleSwitcher } from '@/components/LocaleSwitcher'
import './globals.css'

const outfit = Outfit({ subsets: ['latin'], variable: '--font-outfit' })
const cormorant = Cormorant_Garamond({
  weight: ['400', '500', '600'],
  style: ['normal', 'italic'],
  subsets: ['latin'],
  variable: '--font-cormorant',
})

export const metadata: Metadata = {
  title: 'The Foolish Butcher — Reseller Portal',
  robots: 'noindex, nofollow',
}

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const locale = await getLocale()
  const messages = await getMessages()
  const t = await getTranslations('Layout')

  return (
    <html lang={locale} className={`${outfit.variable} ${cormorant.variable}`}>
      <body style={{ backgroundColor: 'var(--background)', color: 'var(--foreground)', minHeight: '100vh' }}>
        <NextIntlClientProvider messages={messages}>
          <header style={{
            borderBottom: '1px solid var(--border)',
            backgroundColor: 'rgba(8,8,8,0.95)',
            backdropFilter: 'blur(12px)',
            position: 'sticky',
            top: 0,
            zIndex: 40,
            padding: '0 1.5rem',
          }}>
            <div style={{ maxWidth: '72rem', margin: '0 auto', height: '64px', display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <div>
                <span style={{ fontFamily: 'var(--font-cormorant)', fontWeight: 600, fontStyle: 'italic', fontSize: '1.1rem', color: 'var(--accent)', letterSpacing: '0.01em' }}>
                  The Foolish Butcher
                </span>
                <span style={{ fontSize: '0.65rem', textTransform: 'uppercase', letterSpacing: '0.12em', color: 'var(--muted-fg)', display: 'block', marginTop: '-2px' }}>
                  {t('areaRivenditori')}
                </span>
              </div>
              <nav style={{ display: 'flex', gap: '0.25rem', alignItems: 'center' }}>
                {[
                  { href: '/catalogo', label: t('catalogo') },
                  { href: '/carrello', label: t('carrello') },
                  { href: '/account', label: t('account') },
                ].map(item => (
                  <a key={item.href} href={item.href} style={{
                    padding: '0.4rem 0.75rem',
                    fontSize: '0.8rem',
                    color: 'var(--muted-fg)',
                    textDecoration: 'none',
                    borderRadius: '0.5rem',
                    transition: `color var(--dur-fast), background var(--dur-fast)`,
                    letterSpacing: '0.04em',
                  }}>
                    {item.label}
                  </a>
                ))}
                <a href="/api/auth/logout" style={{
                  padding: '0.4rem 0.75rem',
                  fontSize: '0.75rem',
                  color: 'var(--muted-fg)',
                  textDecoration: 'none',
                  borderRadius: '0.5rem',
                  opacity: 0.5,
                  letterSpacing: '0.04em',
                }}>
                  {t('esci')}
                </a>
                <LocaleSwitcher />
              </nav>
            </div>
          </header>
          <main style={{ maxWidth: '72rem', margin: '0 auto', padding: '2.5rem 1.5rem' }}>
            {children}
          </main>
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 3: Typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add b2b/src/components/LocaleSwitcher.tsx b2b/src/app/layout.tsx
git commit -m "feat(b2b/i18n): LocaleSwitcher + translated layout"
```

---

## Task 4: Translate server-side pages

**Files:**
- Modify: `b2b/src/app/catalogo/page.tsx`
- Modify: `b2b/src/app/checkout/conferma/page.tsx`

- [ ] **Step 1: Update catalogo/page.tsx**

Replace `b2b/src/app/catalogo/page.tsx` with:

```tsx
import { fetchResellerProducts } from '@/lib/cms'
import { ProductCard } from '@/components/ProductCard'
import { KitCard } from '@/components/KitCard'
import { getTranslations, getLocale } from 'next-intl/server'

export const dynamic = 'force-dynamic'

export default async function CatalogoPage() {
  const locale = await getLocale()
  const products = await fetchResellerProducts(locale)
  const t = await getTranslations('Catalogo')

  return (
    <div>
      {/* ── HERO ── */}
      <section style={{ marginBottom: '4rem', paddingBottom: '3rem', borderBottom: '1px solid var(--border)' }}>
        <p style={{
          fontSize: '0.65rem', textTransform: 'uppercase', letterSpacing: '0.18em',
          color: 'var(--accent)', marginBottom: '1rem',
        }}>
          {t('portaleRivenditori')}
        </p>
        <h1 style={{
          fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600,
          fontSize: 'clamp(2rem, 5vw, 3.5rem)', lineHeight: 1.1,
          color: 'var(--foreground)', marginBottom: '1.5rem', maxWidth: '28rem',
        }}>
          {t('heroTitle')}
        </h1>
        <p style={{
          fontSize: '0.9rem', color: 'var(--muted-fg)', lineHeight: 1.75,
          maxWidth: '38rem', marginBottom: '2.5rem',
        }}>
          {t('heroBody')}{' '}
          <em style={{ color: 'var(--foreground)', fontStyle: 'italic' }}>{t('habitueSuffix')}</em>.
        </p>

        {/* Pillar cards */}
        <div style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
          gap: '1px',
          background: 'var(--border)',
          border: '1px solid var(--border)',
          borderRadius: '1rem',
          overflow: 'hidden',
        }}>
          {(['p1', 'p2', 'p3'] as const).map((key) => (
            <div key={key} style={{ background: 'var(--card)', padding: '1.75rem 1.5rem' }}>
              <span aria-hidden="true" style={{ fontSize: '1.1rem', color: 'var(--accent)', display: 'block', marginBottom: '0.875rem' }}>
                {t(`pillars.${key}icon`)}
              </span>
              <p style={{ fontSize: '0.875rem', fontWeight: 600, color: 'var(--foreground)', marginBottom: '0.5rem' }}>
                {t(`pillars.${key}title`)}
              </p>
              <p style={{ fontSize: '0.8rem', color: 'var(--muted-fg)', lineHeight: 1.65 }}>
                {t(`pillars.${key}body`)}
              </p>
            </div>
          ))}
        </div>
      </section>

      {/* ── CATALOGO ── */}
      {products.length === 0 ? (
        <div style={{ textAlign: 'center', padding: '4rem 0', color: 'var(--muted-fg)' }}>
          <p>{t('nessuniProdotti')}</p>
        </div>
      ) : (
        <div style={{ display: 'flex', flexDirection: 'column', gap: '4rem' }}>
          {(
            [
              { key: 'tattoo', label: 'Tattoo', descKey: 'tattooDesc' },
              { key: 'pmu', label: 'PMU', descKey: 'pmuDesc' },
            ] as const
          ).map(({ key, label, descKey }) => {
            const section = products.filter(p => p.section === key)
            if (section.length === 0) return null
            return (
              <section key={key}>
                <div style={{ display: 'flex', alignItems: 'baseline', gap: '1rem', marginBottom: '0.5rem' }}>
                  <h2 style={{
                    fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600,
                    fontSize: '1.75rem', color: 'var(--foreground)',
                  }}>
                    {label}
                  </h2>
                  <span style={{ fontSize: '0.65rem', textTransform: 'uppercase', letterSpacing: '0.12em', color: 'var(--accent)' }}>
                    {section.length === 1 ? `1 ${t('prodotto')}` : `${section.length} ${t('prodotti')}`}
                  </span>
                </div>
                <p style={{ fontSize: '0.8rem', color: 'var(--muted-fg)', marginBottom: '1.5rem' }}>
                  {t(descKey)}
                </p>
                <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(240px, 1fr))', gap: '1.25rem' }}>
                  {section.map(p => <ProductCard key={p.id} product={p} />)}
                </div>
              </section>
            )
          })}
        </div>
      )}

      {/* ── KIT RIVENDITORI ── */}
      {(() => {
        const kits = products.filter(p => p.section === 'kit')
        if (kits.length === 0) return null
        return (
          <section style={{ marginTop: '4rem', paddingTop: '3rem', borderTop: '1px solid var(--border)' }}>
            <div style={{ display: 'flex', alignItems: 'baseline', gap: '1rem', marginBottom: '0.5rem' }}>
              <h2 style={{
                fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600,
                fontSize: '1.75rem', color: 'var(--foreground)',
              }}>
                {t('kitTitle')}
              </h2>
              <span style={{ fontSize: '0.65rem', textTransform: 'uppercase', letterSpacing: '0.12em', color: 'var(--accent)' }}>
                {t('kitBadge')}
              </span>
            </div>
            <p style={{ fontSize: '0.8rem', color: 'var(--muted-fg)', marginBottom: '1.75rem' }}>
              {t('kitDesc')}
            </p>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(260px, 1fr))', gap: '1.5rem' }}>
              {kits.map((kit, i) => <KitCard key={kit.id} product={kit} featured={i === 1} />)}
            </div>
          </section>
        )
      })()}

      {/* ── INFO RIVENDITORI ── */}
      <section style={{ marginTop: '5rem', paddingTop: '3.5rem', borderTop: '1px solid var(--border)' }}>
        <p style={{ fontSize: '0.65rem', textTransform: 'uppercase', letterSpacing: '0.18em', color: 'var(--accent)', marginBottom: '2.5rem' }}>
          {t('infoTitle')}
        </p>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '2rem' }}>
          <div>
            <h3 style={{ fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600, fontSize: '1.25rem', color: 'var(--foreground)', marginBottom: '1rem' }}>
              {t('info1Title')}
            </h3>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8 }}>{t('info1Body1')}</p>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8, marginTop: '0.875rem' }}>
              {t('info1Body2')}{' '}
              <span style={{ color: 'var(--foreground)', fontWeight: 500 }}>{t('info1Days')}</span>
              {t('info1Body3')}
            </p>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8, marginTop: '0.875rem' }}>{t('info1Body4')}</p>
          </div>
          <div>
            <h3 style={{ fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600, fontSize: '1.25rem', color: 'var(--foreground)', marginBottom: '1rem' }}>
              {t('info2Title')}
            </h3>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8 }}>{t('info2Body')}</p>
          </div>
          <div>
            <h3 style={{ fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600, fontSize: '1.25rem', color: 'var(--foreground)', marginBottom: '1rem' }}>
              {t('info3Title')}
            </h3>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8 }}>{t('info3Body1')}</p>
            <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.8, marginTop: '0.875rem' }}>{t('info3Body2')}</p>
          </div>
        </div>
      </section>
    </div>
  )
}
```

- [ ] **Step 2: Update checkout/conferma/page.tsx**

Replace `b2b/src/app/checkout/conferma/page.tsx` with:

```tsx
import { getTranslations } from 'next-intl/server'

export default async function ConfermaPage({
  searchParams,
}: {
  searchParams: Promise<{ ordine?: string; metodo?: string }>
}) {
  const { ordine, metodo } = await searchParams
  const isBonifico = metodo === 'bonifico'
  const t = await getTranslations('Conferma')

  return (
    <div style={{ maxWidth: '32rem', margin: '0 auto', marginTop: '4rem', textAlign: 'center' }}>
      <div style={{ fontSize: '4rem', color: 'var(--accent)', marginBottom: '1rem', lineHeight: 1 }}>✓</div>
      <h1 style={{ fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600, fontSize: '2rem', marginBottom: '0.5rem' }}>
        {t('ordineConfermato')}
      </h1>
      <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', marginBottom: '2rem' }}>
        {t('numeroOrdine')} <strong style={{ color: 'var(--foreground)' }}>{ordine}</strong>
      </p>

      {isBonifico ? (
        <div style={{ background: 'rgba(200,169,126,0.06)', border: '1px solid rgba(200,169,126,0.2)', borderRadius: '1rem', padding: '1.5rem', textAlign: 'left', fontSize: '0.875rem' }}>
          <p style={{ fontWeight: 500, marginBottom: '1rem', color: 'var(--foreground)' }}>{t('coordinateBonifico')}</p>
          <p style={{ marginBottom: '0.375rem', color: 'var(--muted-fg)' }}>
            {t('intestatario')} <span style={{ color: 'var(--foreground)', fontWeight: 500 }}>The Foolish Butcher Srl</span>
          </p>
          <p style={{ marginBottom: '0.375rem', color: 'var(--muted-fg)' }}>
            {t('iban')} <span style={{ fontFamily: 'monospace', color: 'var(--accent)', fontWeight: 500, letterSpacing: '0.04em' }}>LT62 3250 0124 6419 4276</span>
          </p>
          <p style={{ marginTop: '1rem', fontSize: '0.75rem', color: 'var(--muted-fg)' }}>
            {t('causale', { ordine: ordine ?? '' })}
          </p>
        </div>
      ) : (
        <p style={{ fontSize: '0.875rem', color: 'var(--muted-fg)' }}>{t('pagamentoRicevuto')}</p>
      )}

      <p style={{ fontSize: '0.8rem', color: 'var(--muted-fg)', marginTop: '1.5rem', opacity: 0.7 }}>
        {t('emailConferma')}
      </p>

      <a
        href="/catalogo"
        style={{
          display: 'inline-block',
          marginTop: '2.5rem',
          border: '1px solid var(--border)',
          borderRadius: '0.75rem',
          padding: '0.6rem 1.5rem',
          fontSize: '0.85rem',
          color: 'var(--foreground)',
          textDecoration: 'none',
        }}
      >
        {t('continuaAcquistare')}
      </a>
    </div>
  )
}
```

- [ ] **Step 3: Typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add "b2b/src/app/catalogo/page.tsx" b2b/src/app/checkout/conferma/page.tsx
git commit -m "feat(b2b/i18n): translate catalogo and conferma server pages"
```

---

## Task 5: Translate client pages

**Files:**
- Modify: `b2b/src/app/login/page.tsx`
- Modify: `b2b/src/app/carrello/page.tsx`
- Modify: `b2b/src/app/checkout/page.tsx`
- Modify: `b2b/src/app/checkout/stripe-pay/page.tsx`
- Modify: `b2b/src/app/account/page.tsx`

- [ ] **Step 1: Update login/page.tsx**

Replace the file with:

```tsx
'use client'
import { useState, Suspense } from 'react'
import { useSearchParams } from 'next/navigation'
import { useTranslations } from 'next-intl'

function LoginForm() {
  const searchParams = useSearchParams()
  const errorKey = searchParams.get('error')
  const [email, setEmail] = useState('')
  const [sent, setSent] = useState(false)
  const [loading, setLoading] = useState(false)
  const t = useTranslations('Login')

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setLoading(true)
    try {
      await fetch('/api/auth/magic-link', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email }),
      })
      setSent(true)
    } finally {
      setLoading(false)
    }
  }

  const errorMsg = errorKey
    ? (t.raw(`errors.${errorKey}`) as string | undefined ?? t('errors.unknown'))
    : null

  return (
    <div style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '2rem' }}>
      <div style={{ width: '100%', maxWidth: '400px' }}>
        <div style={{ textAlign: 'center', marginBottom: '3rem' }}>
          <h1 style={{ fontFamily: 'var(--font-cormorant)', fontWeight: 600, fontStyle: 'italic', fontSize: '2rem', color: 'var(--accent)', marginBottom: '0.25rem' }}>
            The Foolish Butcher
          </h1>
          <p style={{ fontSize: '0.7rem', textTransform: 'uppercase', letterSpacing: '0.15em', color: 'var(--muted-fg)' }}>
            {t('portaleRivenditori')}
          </p>
        </div>

        <div style={{ background: 'var(--card)', border: '1px solid var(--border)', borderRadius: '1rem', padding: '2rem' }}>
          {errorMsg && (
            <div style={{ background: 'rgba(192,57,43,0.1)', border: '1px solid rgba(192,57,43,0.3)', borderRadius: '0.5rem', padding: '0.75rem 1rem', marginBottom: '1.25rem', fontSize: '0.85rem', color: '#e57373' }}>
              {errorMsg}
            </div>
          )}

          {sent ? (
            <div style={{ textAlign: 'center', padding: '1rem 0' }}>
              <div style={{ fontSize: '2rem', marginBottom: '1rem' }}>✓</div>
              <p style={{ color: 'var(--accent)', fontWeight: 500, marginBottom: '0.5rem' }}>{t('emailInviata')}</p>
              <p style={{ fontSize: '0.85rem', color: 'var(--muted-fg)', lineHeight: 1.5 }}>{t('emailInviataDesc')}</p>
            </div>
          ) : (
            <>
              <h2 style={{ fontSize: '1rem', fontWeight: 500, marginBottom: '0.375rem' }}>{t('accedi')}</h2>
              <p style={{ fontSize: '0.8rem', color: 'var(--muted-fg)', marginBottom: '1.5rem', lineHeight: 1.5 }}>{t('desc')}</p>
              <form onSubmit={handleSubmit} style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
                <input
                  type="email"
                  required
                  placeholder={t('placeholder')}
                  value={email}
                  onChange={e => setEmail(e.target.value)}
                  style={{
                    background: 'var(--background)',
                    border: '1px solid var(--border)',
                    borderRadius: '0.75rem',
                    padding: '0.75rem 1rem',
                    fontSize: '0.9rem',
                    color: 'var(--foreground)',
                    outline: 'none',
                    width: '100%',
                  }}
                />
                <button
                  type="submit"
                  disabled={loading}
                  style={{
                    background: 'var(--accent)',
                    color: '#000',
                    border: 'none',
                    borderRadius: '0.75rem',
                    padding: '0.75rem 1.5rem',
                    fontSize: '0.875rem',
                    fontWeight: 600,
                    cursor: loading ? 'not-allowed' : 'pointer',
                    opacity: loading ? 0.6 : 1,
                    transition: `opacity var(--dur-fast)`,
                    width: '100%',
                    letterSpacing: '0.02em',
                  }}
                >
                  {loading ? t('invioInCorso') : t('riceviLink')}
                </button>
              </form>
            </>
          )}
        </div>

        <p style={{ textAlign: 'center', marginTop: '1.5rem', fontSize: '0.75rem', color: 'var(--muted-fg)', opacity: 0.6 }}>
          {t('accessoRiservato')}
        </p>
      </div>
    </div>
  )
}

export default function LoginPage() {
  return (
    <Suspense>
      <LoginForm />
    </Suspense>
  )
}
```

- [ ] **Step 2: Update carrello/page.tsx**

Replace the file with:

```tsx
'use client'
import { useCart } from '@/lib/cart'
import { calculateLineTotal, formatPrice } from '@/lib/pricing'
import { useTranslations } from 'next-intl'
import Link from 'next/link'

export default function CarrelloPage() {
  const { items, updateQty, removeItem, total } = useCart()
  const t = useTranslations('Carrello')

  if (items.length === 0) {
    return (
      <div style={{ textAlign: 'center', padding: '5rem 0' }}>
        <p style={{ color: 'var(--muted-fg)', marginBottom: '1.5rem' }}>{t('vuoto')}</p>
        <Link href="/catalogo" style={{
          display: 'inline-block',
          border: '1px solid var(--border)',
          borderRadius: '0.75rem',
          padding: '0.6rem 1.25rem',
          fontSize: '0.85rem',
          color: 'var(--foreground)',
          textDecoration: 'none',
        }}>
          {t('tornaCatalogo')}
        </Link>
      </div>
    )
  }

  return (
    <div style={{ maxWidth: '680px' }}>
      <h1 style={{ fontFamily: 'var(--font-cormorant)', fontStyle: 'italic', fontWeight: 600, fontSize: '2rem', marginBottom: '2rem' }}>
        {t('titolo')}
      </h1>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '0' }}>
        {items.map((item, idx) => {
          const lineTotal = calculateLineTotal(item.unitPrice, item.qty, item.priceTiers)
          return (
            <div key={item.variantSku} style={{
              display: 'flex',
              alignItems: 'center',
              gap: '1rem',
              padding: '1.25rem 0',
              borderTop: idx > 0 ? '1px solid var(--border)' : undefined,
            }}>
              <div style={{ flex: 1, minWidth: 0 }}>
                <p style={{ fontWeight: 500, fontSize: '0.9rem', marginBottom: '0.2rem' }}>{item.productName}</p>
                <p style={{ fontSize: '0.78rem', color: 'var(--muted-fg)' }}>{item.variantLabel}</p>
              </div>
              <div style={{ display: 'flex', alignItems: 'center', border: '1px solid var(--border)', borderRadius: '0.5rem', overflow: 'hidden' }}>
                <button onClick={() => updateQty(item.variantSku, item.qty - 1)} style={{ padding: '0.4rem 0.75rem', background: 'transparent', border: 'none', color: 'var(--foreground)', cursor: 'pointer', fontSize: '1rem' }}>−</button>
                <span style={{ padding: '0.4rem 0.5rem', fontSize: '0.85rem', minWidth: '2rem', textAlign: 'center' }}>{item.qty}</span>
                <button onClick={() => updateQty(item.variantSku, item.qty + 1)} style={{ padding: '0.4rem 0.75rem', background: 'transparent', border: 'none', color: 'var(--foreground)', cursor: 'pointer', fontSize: '1rem' }}>+</button>
              </div>
              <div style={{ fontSize: '0.9rem', fontWeight: 500, minWidth: '80px', textAlign: 'right', color: 'var(--accent)' }}>
                {formatPrice(lineTotal)}
              </div>
              <button onClick={() => removeItem(item.variantSku)} style={{ background: 'transparent', border: 'none', color: 'var(--muted-fg)', cursor: 'pointer', fontSize: '0.75rem', opacity: 0.5, padding: '0.25rem' }}>✕</button>
            </div>
          )
        })}
      </div>

      <div style={{ borderTop: '1px solid var(--border)', marginTop: '0.5rem', paddingTop: '1.5rem', display: 'flex', alignItems: 'flex-end', justifyContent: 'space-between' }}>
        <div>
          <p style={{ fontSize: '0.75rem', color: 'var(--muted-fg)', marginBottom: '0.25rem' }}>{t('totaleIva')}</p>
          <p style={{ fontSize: '1.75rem', fontWeight: 600, color: 'var(--accent)', fontFamily: 'var(--font-cormorant)', fontStyle: 'italic' }}>{formatPrice(total())}</p>
          <p style={{ fontSize: '0.75rem', color: 'var(--muted-fg)', marginTop: '0.25rem' }}>{t('spedizione')}</p>
        </div>
        <Link href="/checkout" style={{
          background: 'var(--accent)',
          color: '#000',
          borderRadius: '0.75rem',
          padding: '0.75rem 1.75rem',
          fontSize: '0.875rem',
          fontWeight: 600,
          textDecoration: 'none',
          letterSpacing: '0.02em',
        }}>
          {t('checkout')}
        </Link>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Update checkout/page.tsx**

In `b2b/src/app/checkout/page.tsx`, add `useTranslations` import and replace all hardcoded strings. Add at the top of the file (after existing imports):

```tsx
import { useTranslations } from 'next-intl'
```

Inside `CheckoutPage()` add after existing state declarations:

```tsx
const t = useTranslations('Checkout')
```

Then replace every hardcoded string:
- `'Checkout'` → `t('titolo')` (h1)
- `'Dati fatturazione'` → `t('fatturazione')` (h2)
- `'Ragione sociale *'` → `t('ragioneSociale')`
- `'P.IVA *'` → `t('piva')`
- `'Codice SDI / PEC *'` → `t('sdi')`
- `placeholder="0000000 o pec@email.it"` → `placeholder={t('sdiPlaceholder')}`
- `'Indirizzo di fatturazione *'` → `t('indirizzoBilling')`
- `'Città *'` → `t('citta')` (×2)
- `'CAP *'` → `t('cap')` (×2)
- `'Indirizzo di spedizione'` → `t('spedizione')` (h2)
- `'Nome / Ragione sociale destinatario *'` → `t('nomeDestinatario')`
- `'Indirizzo *'` → `t('indirizzoSpedizione')`
- `'Metodo di pagamento'` → `t('pagamento')` (h2)
- `'Bonifico bancario'` → `t('bonifico')`
- `'Riceverai le coordinate al momento della conferma'` → `t('bonificoDesc')`
- `'Carta di credito (Stripe)'` → `t('carta')`
- The carta description paragraph → `t('cartaDesc', { price: formatPrice(stripeTotal) })`
- `'Note (opzionale)'` → `t('note')`
- `placeholder="Note per l'ordine..."` → `placeholder={t('notePlaceholder')}`
- `'Subtotale prodotti'` → `t('subtotale')`
- `'Supplemento carta (4%)'` → `t('supplementoCarta')`
- `'Spedizione'` → `t('costoSpedizione')`
- `'Da definire'` → `t('daDefinire')`
- `'Totale'` → `t('totale')`
- `'Elaborazione...'` → `t('elaborazione')`
- `'Conferma ordine (bonifico bancario)'` → `t('confermaBonifico')`
- `'Paga con carta →'` → `t('pagaCarta')`
- `alert('Errore...')` → `alert(t('errorOrdine'))`

- [ ] **Step 4: Update checkout/stripe-pay/page.tsx**

Add to imports:

```tsx
import { useTranslations, useLocale } from 'next-intl'
```

In `PayForm`: add `const t = useTranslations('StripePay')` and replace strings:
- `'Errore pagamento'` → `t('errore')`
- `'Elaborazione...'` → `t('elaborazione')`
- `'Paga ora'` → `t('pagaOra')`

In `StripePayPage`: add `const t = useTranslations('StripePay')` and `const locale = useLocale()`, replace:
- `<p ...>Caricamento...</p>` → `<p ...>{t('caricamento')}</p>`
- h1 `'Pagamento con carta'` → `t('titolo')`
- `locale: 'it'` inside `Elements` options → `locale: locale as 'it' | 'fr' | 'en' | 'es'`

- [ ] **Step 5: Update account/page.tsx**

Add import:

```tsx
import { useTranslations } from 'next-intl'
```

Add inside `AccountPage()`:

```tsx
const t = useTranslations('Account')
```

Replace all hardcoded strings using the `Account` namespace keys. The `stateLabels` record becomes:

```tsx
const stateLabels: Record<string, string> = {
  received: t('states.received'),
  in_production: t('states.in_production'),
  shipped: t('states.shipped'),
  delivered: t('states.delivered'),
}
```

Replace:
- `'Caricamento...'` → `t('caricamento')`
- `'Il mio account'` → `t('titolo')`
- `'Profilo aziendale'` → `t('profiloAziendale')`
- `'Ragione sociale'` → `t('ragioneSociale')`
- `'Referente'` → `t('referente')`
- `'Email'` → `t('email')`
- `'P.IVA'` → `t('piva')`
- `'Storico ordini'` → `t('storicoOrdini')`
- `'Nessun ordine ancora.'` → `t('nessunoOrdine')`
- `'Tracking: '` → `t('tracking') + ' '`
- Date locale `.toLocaleDateString('it-IT')` → `.toLocaleDateString(locale)` (add `const locale = useLocale()` from next-intl)

- [ ] **Step 6: Typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add b2b/src/app/login/page.tsx b2b/src/app/carrello/page.tsx b2b/src/app/checkout/page.tsx b2b/src/app/checkout/stripe-pay/page.tsx b2b/src/app/account/page.tsx
git commit -m "feat(b2b/i18n): translate all client pages"
```

---

## Task 6: Translate UI components

**Files:**
- Modify: `b2b/src/components/ProductCard.tsx`
- Modify: `b2b/src/components/KitCard.tsx`
- Modify: `b2b/src/components/PriceTierTable.tsx`

- [ ] **Step 1: Update ProductCard.tsx**

Add import and `useTranslations('ProductCard')`. Replace:
- `'Nessuna immagine'` → `t('nessunaImmagine')`
- `Da ${formatPrice(...)}` → `` `${t('da')} ${formatPrice(...)}${t('alPezzo')}` `` (remove hardcoded `/pz`)
- `Fino a -${maxDiscount}%` → `t('finoA', { n: maxDiscount })`

- [ ] **Step 2: Update KitCard.tsx**

Add `useTranslations('KitCard')`. Replace:
- `'Kit rivenditori'` → `t('kitRivenditori')`
- `'Più richiesto'` → `t('piuRichiesto')`
- `'✓ Aggiunto al carrello'` → `t('aggiunto')`
- `'Aggiungi al carrello'` → `t('aggiungiCarrello')`

- [ ] **Step 3: Update PriceTierTable.tsx**

This is a Server Component — use `getTranslations` from `next-intl/server`. Add `async` to the function signature:

```tsx
import { getTranslations } from 'next-intl/server'

export async function PriceTierTable({ tiers, basePrice, currentQty = 0 }: Props) {
  if (!tiers || tiers.length === 0) return null
  const t = await getTranslations('PriceTierTable')
  // ...replace all strings
```

Replace:
- `'Prezzi a volume'` → `t('prezziVolume')`
- Headers `['Quantità', 'Sconto', 'Prezzo/pz']` → `[t('quantita'), t('sconto'), t('prezzoPz')]`
- `` `${tier.minQty}–${tier.maxQty} pz` `` → `t('rangePz', { min: tier.minQty, max: tier.maxQty })`
- `` `${tier.minQty}+ pz` `` → `t('minPlusPz', { min: tier.minQty })`

**Note:** Since `PriceTierTable` becomes `async`, any caller that passes it as a JSX element remains unchanged — Next.js handles async Server Components transparently.

- [ ] **Step 4: Typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add b2b/src/components/ProductCard.tsx b2b/src/components/KitCard.tsx b2b/src/components/PriceTierTable.tsx
git commit -m "feat(b2b/i18n): translate UI components"
```

---

## Task 7: CMS locale passthrough

**Files:**
- Modify: `b2b/src/lib/cms.ts`
- Modify: `b2b/src/app/api/catalog/route.ts`
- Modify: `b2b/src/app/catalogo/[slug]/page.tsx`

- [ ] **Step 1: Update cms.ts to accept locale**

In `b2b/src/lib/cms.ts`, update both fetch functions to accept and pass a locale:

```ts
export async function fetchResellerProducts(locale = 'it'): Promise<ResellerProduct[]> {
  const url = `${CMS_URL}/api/products?where[resellerVisible][equals]=true&depth=1&limit=100&locale=${locale}`
  // rest unchanged
}

export async function fetchResellerProductBySlug(slug: string, locale = 'it'): Promise<ResellerProduct | null> {
  const url = `${CMS_URL}/api/products?where[slug][equals]=${slug}&where[resellerVisible][equals]=true&depth=1&limit=1&locale=${locale}`
  // rest unchanged
}
```

- [ ] **Step 2: Update api/catalog/route.ts**

```ts
import { NextRequest, NextResponse } from 'next/server'
import { fetchResellerProducts, fetchResellerProductBySlug } from '@/lib/cms'

const VALID_LOCALES = ['it', 'fr', 'en', 'es']

export async function GET(req: NextRequest) {
  const slug = req.nextUrl.searchParams.get('slug')
  const localeParam = req.nextUrl.searchParams.get('locale')
  const cookieLocale = req.cookies.get('NEXT_LOCALE')?.value
  const locale = VALID_LOCALES.includes(localeParam ?? '') ? localeParam!
    : VALID_LOCALES.includes(cookieLocale ?? '') ? cookieLocale!
    : 'it'

  if (slug) {
    const product = await fetchResellerProductBySlug(slug, locale)
    if (!product) return NextResponse.json(null, { status: 404 })
    return NextResponse.json(product)
  }

  const products = await fetchResellerProducts(locale)
  return NextResponse.json(products)
}
```

- [ ] **Step 3: Update catalogo/[slug]/page.tsx to pass locale**

In the `fetch` call inside the `useEffect`, append the locale:

```tsx
import { useLocale } from 'next-intl'
// inside component:
const locale = useLocale()
// inside useEffect:
fetch(`/api/catalog?slug=${slug}&locale=${locale}`)
```

Also add `useTranslations('ProductPage')` and replace:
- `'← Catalogo'` → `t('tornaCatalogo')`
- `'Prodotto non trovato.'` → `t('prodottoNonTrovato')`
- `'Variante'` → `t('variante')`
- `'Quantità'` → `t('quantita')`
- `'✓ Aggiunto!'` → `t('aggiunto')`
- `'Aggiungi al carrello'` → `t('aggiungiCarrello')`
- `'Caricamento...'` (loading skeleton) → `t('caricamento')`

- [ ] **Step 4: Final typecheck**

```bash
cd b2b && npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 5: Commit and push**

```bash
git add b2b/src/lib/cms.ts "b2b/src/app/api/catalog/route.ts" "b2b/src/app/catalogo/[slug]/page.tsx"
git commit -m "feat(b2b/i18n): CMS locale passthrough — product names in user language"
git push origin main
```

---

## Verification

After deploy, verify:

1. Open `https://rivenditori.thefoolishbutcher.com` — browser in French → UI in French automatically
2. Switch language via IT/FR/EN/ES buttons → UI language changes immediately
3. Navigate to a product → name/description in selected language (if CMS has translations)
4. Go through cart → checkout → confirmation: all text in selected language
5. `html lang` attribute matches selected locale (inspect element)
6. Cookie `NEXT_LOCALE` is set after switching language
